<UserControl x:Class="Echo.Views.GroupDialogView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    d:DesignHeight="480" d:DesignWidth="480">

    <StackPanel Background="{StaticResource PhoneChromeBrush}">
        <TextBlock Style="{StaticResource PhoneTextLargeStyle}">Create new group</TextBlock>
        <TextBlock Style="{StaticResource PhoneTextNormalStyle}">Enter group name</TextBlock>
        <TextBox Name="GroupName" />
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>
            <Button Name="Confirm"
                    Content="confirm"
                    Grid.Column="0" />
            <Button Name="TryClose"
                    Content="cancel"
                    Grid.Column="1" />
        </Grid>
    </StackPanel>
</UserControl>
